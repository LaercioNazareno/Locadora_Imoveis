@model IEnumerable<locadora.Models.Contrato>

@{
    ViewData["Title"] = "Index";
}

<h2>Contrato</h2>

<p>
    <a asp-action="Details">Novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Locatario.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Imovel.ValorAluguel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataEncerramento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model) {
        <tr>
            <td>
                <a asp-action="Details" asp-route-id="@item.Id">
                    @Html.DisplayFor(modelItem => item.Locatario.Nome)
                </a>
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Imovel.ValorAluguel)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataEncerramento)
            </td>
            <td> 
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
    }
    </tbody>
</table>